// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.unibas.medizin.osce.domain;

import ch.unibas.medizin.osce.domain.RoleTopicDataOnDemand;
import ch.unibas.medizin.osce.domain.StandardizedRole;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect StandardizedRoleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: StandardizedRoleDataOnDemand: @Component;
    
    private Random StandardizedRoleDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<StandardizedRole> StandardizedRoleDataOnDemand.data;
    
    @Autowired
    private RoleTopicDataOnDemand StandardizedRoleDataOnDemand.roleTopicDataOnDemand;
    
    public StandardizedRole StandardizedRoleDataOnDemand.getNewTransientStandardizedRole(int index) {
        ch.unibas.medizin.osce.domain.StandardizedRole obj = new ch.unibas.medizin.osce.domain.StandardizedRole();
        setShortName(obj, index);
        setLongName(obj, index);
        setCaseDescription(obj, index);
        setRoleScript(obj, index);
        setRoleType(obj, index);
        setActive(obj, index);
        setRoleTopic(obj, index);
        setPreviousVersion(obj, index);
        setStudyYear(obj, index);
        setMainVersion(obj, index);
        setSubVersion(obj, index);
        setCheckList(obj, index);
        return obj;
    }
    
    private void StandardizedRoleDataOnDemand.setShortName(StandardizedRole obj, int index) {
        java.lang.String shortName = "shortName_" + index;
        if (shortName.length() > 20) {
            shortName = shortName.substring(0, 20);
        }
        obj.setShortName(shortName);
    }
    
    private void StandardizedRoleDataOnDemand.setLongName(StandardizedRole obj, int index) {
        java.lang.String longName = "longName_" + index;
        if (longName.length() > 100) {
            longName = longName.substring(0, 100);
        }
        obj.setLongName(longName);
    }
    
    private void StandardizedRoleDataOnDemand.setCaseDescription(StandardizedRole obj, int index) {
        java.lang.String caseDescription = "caseDescription_" + index;
        if (caseDescription.length() > 999) {
            caseDescription = caseDescription.substring(0, 999);
        }
        obj.setCaseDescription(caseDescription);
    }
    
    private void StandardizedRoleDataOnDemand.setRoleScript(StandardizedRole obj, int index) {
        java.lang.String roleScript = "roleScript_" + index;
        if (roleScript.length() > 255) {
            roleScript = roleScript.substring(0, 255);
        }
        obj.setRoleScript(roleScript);
    }
    
    private void StandardizedRoleDataOnDemand.setRoleType(StandardizedRole obj, int index) {
        ch.unibas.medizin.osce.shared.RoleTypes roleType = ch.unibas.medizin.osce.shared.RoleTypes.class.getEnumConstants()[0];
        obj.setRoleType(roleType);
    }
    
    private void StandardizedRoleDataOnDemand.setActive(StandardizedRole obj, int index) {
        java.lang.Boolean active = Boolean.TRUE;
        obj.setActive(active);
    }
    
    private void StandardizedRoleDataOnDemand.setRoleTopic(StandardizedRole obj, int index) {
        ch.unibas.medizin.osce.domain.RoleTopic roleTopic = roleTopicDataOnDemand.getRandomRoleTopic();
        obj.setRoleTopic(roleTopic);
    }
    
    private void StandardizedRoleDataOnDemand.setPreviousVersion(StandardizedRole obj, int index) {
        ch.unibas.medizin.osce.domain.StandardizedRole previousVersion = obj;
        obj.setPreviousVersion(previousVersion);
    }
    
    private void StandardizedRoleDataOnDemand.setStudyYear(StandardizedRole obj, int index) {
        ch.unibas.medizin.osce.shared.StudyYears studyYear = ch.unibas.medizin.osce.shared.StudyYears.class.getEnumConstants()[0];
        obj.setStudyYear(studyYear);
    }
    
    private void StandardizedRoleDataOnDemand.setMainVersion(StandardizedRole obj, int index) {
        java.lang.Integer mainVersion = new Integer(index);
        obj.setMainVersion(mainVersion);
    }
    
    private void StandardizedRoleDataOnDemand.setSubVersion(StandardizedRole obj, int index) {
        java.lang.Integer subVersion = new Integer(index);
        obj.setSubVersion(subVersion);
    }
    
    private void StandardizedRoleDataOnDemand.setCheckList(StandardizedRole obj, int index) {
        ch.unibas.medizin.osce.domain.CheckList checkList = null;
        obj.setCheckList(checkList);
    }
    
    public StandardizedRole StandardizedRoleDataOnDemand.getSpecificStandardizedRole(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        StandardizedRole obj = data.get(index);
        return StandardizedRole.findStandardizedRole(obj.getId());
    }
    
    public StandardizedRole StandardizedRoleDataOnDemand.getRandomStandardizedRole() {
        init();
        StandardizedRole obj = data.get(rnd.nextInt(data.size()));
        return StandardizedRole.findStandardizedRole(obj.getId());
    }
    
    public boolean StandardizedRoleDataOnDemand.modifyStandardizedRole(StandardizedRole obj) {
        return false;
    }
    
    public void StandardizedRoleDataOnDemand.init() {
        data = ch.unibas.medizin.osce.domain.StandardizedRole.findStandardizedRoleEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'StandardizedRole' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<ch.unibas.medizin.osce.domain.StandardizedRole>();
        for (int i = 0; i < 10; i++) {
            ch.unibas.medizin.osce.domain.StandardizedRole obj = getNewTransientStandardizedRole(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
