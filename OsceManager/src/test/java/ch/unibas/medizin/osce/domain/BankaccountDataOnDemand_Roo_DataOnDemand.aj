// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package ch.unibas.medizin.osce.domain;

import ch.unibas.medizin.osce.domain.Bankaccount;
import ch.unibas.medizin.osce.domain.NationalityDataOnDemand;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect BankaccountDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BankaccountDataOnDemand: @Component;
    
    private Random BankaccountDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Bankaccount> BankaccountDataOnDemand.data;
    
    @Autowired
    private NationalityDataOnDemand BankaccountDataOnDemand.nationalityDataOnDemand;
    
    public Bankaccount BankaccountDataOnDemand.getNewTransientBankaccount(int index) {
        ch.unibas.medizin.osce.domain.Bankaccount obj = new ch.unibas.medizin.osce.domain.Bankaccount();
        setBankName(obj, index);
        setIBAN(obj, index);
        setBIC(obj, index);
        setOwnerName(obj, index);
        setPostalCode(obj, index);
        setCity(obj, index);
        setCountry(obj, index);
        return obj;
    }
    
    private void BankaccountDataOnDemand.setBankName(Bankaccount obj, int index) {
        java.lang.String bankName = "bankName_" + index;
        if (bankName.length() > 40) {
            bankName = bankName.substring(0, 40);
        }
        obj.setBankName(bankName);
    }
    
    private void BankaccountDataOnDemand.setIBAN(Bankaccount obj, int index) {
        java.lang.String IBAN = "IBAN_" + index;
        if (IBAN.length() > 40) {
            IBAN = IBAN.substring(0, 40);
        }
        obj.setIBAN(IBAN);
    }
    
    private void BankaccountDataOnDemand.setBIC(Bankaccount obj, int index) {
        java.lang.String BIC = "BIC_" + index;
        if (BIC.length() > 40) {
            BIC = BIC.substring(0, 40);
        }
        obj.setBIC(BIC);
    }
    
    private void BankaccountDataOnDemand.setOwnerName(Bankaccount obj, int index) {
        java.lang.String ownerName = "ownerName_" + index;
        if (ownerName.length() > 40) {
            ownerName = ownerName.substring(0, 40);
        }
        obj.setOwnerName(ownerName);
    }
    
    private void BankaccountDataOnDemand.setPostalCode(Bankaccount obj, int index) {
        java.lang.Integer postalCode = new Integer(index);
        obj.setPostalCode(postalCode);
    }
    
    private void BankaccountDataOnDemand.setCity(Bankaccount obj, int index) {
        java.lang.String city = "city_" + index;
        if (city.length() > 30) {
            city = city.substring(0, 30);
        }
        obj.setCity(city);
    }
    
    private void BankaccountDataOnDemand.setCountry(Bankaccount obj, int index) {
        ch.unibas.medizin.osce.domain.Nationality country = nationalityDataOnDemand.getRandomNationality();
        obj.setCountry(country);
    }
    
    public Bankaccount BankaccountDataOnDemand.getSpecificBankaccount(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Bankaccount obj = data.get(index);
        return Bankaccount.findBankaccount(obj.getId());
    }
    
    public Bankaccount BankaccountDataOnDemand.getRandomBankaccount() {
        init();
        Bankaccount obj = data.get(rnd.nextInt(data.size()));
        return Bankaccount.findBankaccount(obj.getId());
    }
    
    public boolean BankaccountDataOnDemand.modifyBankaccount(Bankaccount obj) {
        return false;
    }
    
    public void BankaccountDataOnDemand.init() {
        data = ch.unibas.medizin.osce.domain.Bankaccount.findBankaccountEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Bankaccount' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<ch.unibas.medizin.osce.domain.Bankaccount>();
        for (int i = 0; i < 10; i++) {
            ch.unibas.medizin.osce.domain.Bankaccount obj = getNewTransientBankaccount(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
