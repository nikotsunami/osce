<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel id="boundElementHolder" styleName="{style.outer}">
    
    
    
    

    <g:Button ui:field="delete">Delete</g:Button>
  <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
        <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>
        <div id="semester">
            <div class="{style.label}">Semester</div>
            <div class="{style.value}" ui:field="semester"/>
        </div>
        <div id="standardizedPatient">
            <div class="{style.label}">Standardized Patient</div>
            <div class="{style.value}" ui:field="standardizedPatient"/>
        </div>
        <div id="accepted">
            <div class="{style.label}">Accepted</div>
            <div class="{style.value}" ui:field="accepted"/>
        </div>
        <div id="osceDays">
            <div class="{style.label}">Osce Days</div>
            <div class="{style.value}" ui:field="osceDays"/>
        </div>
        <div id="patientInRole">
            <div class="{style.label}">Patient In Role</div>
            <div class="{style.value}" ui:field="patientInRole"/>
        </div>
        <div id="trainings">
            <div class="{style.label}">Trainings</div>
            <div class="{style.value}" ui:field="trainings"/>
        </div>
    </g:HTMLPanel>
</ui:UiBinder>
