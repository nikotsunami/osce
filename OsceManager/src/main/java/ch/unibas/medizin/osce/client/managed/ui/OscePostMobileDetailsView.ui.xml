<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel id="boundElementHolder" styleName="{style.outer}">
    <div id="isPossibleStart">
            <div class="{style.label}">Is Possible Start</div>
            <div class="{style.value}" ui:field="isPossibleStart"/>
        </div>
    <div id="roleTopic">
            <div class="{style.label}">Role Topic</div>
            <div class="{style.value}" ui:field="roleTopic"/>
        </div>
    <div id="nextPost">
            <div class="{style.label}">Next Post</div>
            <div class="{style.value}" ui:field="nextPost"/>
        </div>
    <div id="oscePostRooms">
            <div class="{style.label}">Osce Post Rooms</div>
            <div class="{style.value}" ui:field="oscePostRooms"/>
        </div>
    <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
    <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>

    <g:Button ui:field="delete">Delete</g:Button>
  </g:HTMLPanel>
</ui:UiBinder>
