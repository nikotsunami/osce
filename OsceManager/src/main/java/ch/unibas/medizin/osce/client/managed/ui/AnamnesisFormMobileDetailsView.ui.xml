<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel id="boundElementHolder" styleName="{style.outer}">
    <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
    <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>
    <div id="createDate">
            <div class="{style.label}">Create Date</div>
            <div class="{style.value}" ui:field="createDate"/>
        </div>
    <div id="anamnesischecksvalues">
            <div class="{style.label}">Anamnesischecksvalues</div>
            <div class="{style.value}" ui:field="anamnesischecksvalues"/>
        </div>
    <div id="scars">
            <div class="{style.label}">Scars</div>
            <div class="{style.value}" ui:field="scars"/>
        </div>

    <g:Button ui:field="delete">Delete</g:Button>
  </g:HTMLPanel>
</ui:UiBinder>
